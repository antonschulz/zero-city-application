import 'package:flutter/material.dart';


class ZoneBase extends StatelessWidget {
  late String name;
  late int id;

  ZoneBase(this.name, this.id, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
  
}